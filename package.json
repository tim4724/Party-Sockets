{
  "name": "party-sockets",
  "version": "2.3.1",
  "description": "Minimal WebSocket relay server for party games",
  "scripts": {
    "start": "bun run server.ts",
    "test": "bun test server.test.ts",
    "test:live": "bun test server.live.test.ts"
  }
}
